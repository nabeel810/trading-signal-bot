name: Trading Signal Bot

on:
  schedule:
    - cron: "*/5 * * * *"   # ظٹط¹ظ…ظ„ ظƒظ„ 5 ط¯ظ‚ط§ط¦ظ‚ طھظ„ظ‚ط§ط¦ظٹط§ظ‹
  workflow_dispatch:         # ظٹط³ظ…ط­ ط¨طھط´ط؛ظٹظ„ظ‡ ظٹط¯ظˆظٹط§ظ‹ ظ…ظ† ظˆط§ط¬ظ‡ط© GitHub ط£ظٹط¶ط§ظ‹

permissions:
  contents: write            # ظ…ط·ظ„ظˆط¨ ظ„ط­ظپط¸ ظ…ظ„ظپ state.json ط¯ط§ط®ظ„ ط§ظ„ظ…ط³طھظˆط¯ط¹

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: طھظ†ط²ظٹظ„ ط§ظ„ظƒظˆط¯
        uses: actions/checkout@v4

      - name: طھط¬ظ‡ظٹط² Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: طھط«ط¨ظٹطھ ط§ظ„ظ…ظƒطھط¨ط§طھ
        run: pip install -r requirements.txt

      - name: طھط´ط؛ظٹظ„ ط§ظ„ط¨ظˆطھ
        env:
          TWELVEDATA_API_KEY: ${{ secrets.TWELVEDATA_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          PAIRS: ${{ secrets.PAIRS }}
          FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
        run: python signal_bot.py

      - name: ط­ظپط¸ ط­ط§ظ„ط© ط¢ط®ط± ط¥ط´ط§ط±ط© (state.json)
        run: |
          git config user.name "signal-bot"
          git config user.email "bot@users.noreply.github.com"
          git add state.json || true
          git diff --quiet && git diff --staged --quiet || git commit -m "طھط­ط¯ظٹط« ط­ط§ظ„ط© ط§ظ„ط¨ظˆطھ [skip ci]"
          git push || true
